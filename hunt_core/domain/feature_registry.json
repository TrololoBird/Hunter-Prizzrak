{
  "schema_version": "2",
  "description": "Hunt feature lake columns — Phase 0.1 gap-close + Tier-1 map/cross facts.",
  "features": {
    "price": {"type": "float", "unit": "usd", "warmup": 0, "source": "row.price", "required": true},
    "close": {"type": "float", "unit": "usd", "warmup": 1, "source": "frame.close", "required": true},
    "rsi14": {"type": "float", "unit": "index_0_100", "warmup": 14, "source": "frame.rsi14", "required": true},
    "atr14": {"type": "float", "unit": "usd", "warmup": 14, "source": "frame.atr14", "required": true},
    "atr_pct": {"type": "float", "unit": "percent", "warmup": 14, "source": "frame.atr_pct", "required": false},
    "adx14": {"type": "float", "unit": "index_0_100", "warmup": 14, "source": "frame.adx14", "required": true},
    "ema20": {"type": "float", "unit": "usd", "warmup": 20, "source": "frame.ema20", "required": false},
    "ema50": {"type": "float", "unit": "usd", "warmup": 50, "source": "frame.ema50", "required": false},
    "ema200": {"type": "float", "unit": "usd", "warmup": 200, "source": "frame.ema200", "required": false},
    "volume_ratio20": {"type": "float", "unit": "ratio", "warmup": 20, "source": "frame.volume_ratio20", "required": false},
    "macd_hist": {"type": "float", "unit": "macd", "warmup": 26, "source": "frame.macd_hist", "required": false},
    "bb_pct_b": {"type": "float", "unit": "ratio", "warmup": 20, "source": "frame.bb_pct_b", "required": false},
    "bb_width": {"type": "float", "unit": "ratio", "warmup": 20, "source": "frame.bb_width", "required": false},
    "supertrend_dir": {"type": "float", "unit": "direction", "warmup": 10, "source": "frame.supertrend_dir", "required": false},
    "delta_ratio": {"type": "float", "unit": "ratio", "warmup": 5, "source": "frame.delta_ratio", "required": false},
    "zscore30": {"type": "float", "unit": "zscore", "warmup": 30, "source": "frame.zscore30", "required": false},
    "session_cvd": {"type": "float", "unit": "volume", "warmup": 20, "source": "frame.session_cvd", "required": false},
    "rolling_cvd_24h": {"type": "float", "unit": "volume", "warmup": 20, "source": "frame.rolling_cvd_24h", "required": false},
    "chg_24h_pct": {"type": "float", "unit": "percent", "warmup": 0, "source": "row.chg_24h_pct", "required": false},
    "range_24h_pct": {"type": "float", "unit": "percent", "warmup": 0, "source": "row.range_24h_pct", "required": false},
    "oi": {"type": "float", "unit": "contracts", "warmup": 0, "source": "prepared.oi_current", "required": false},
    "oi_change_pct": {"type": "float", "unit": "percent", "warmup": 0, "source": "prepared.oi_change_pct", "required": false},
    "oi_slope_5m": {"type": "float", "unit": "ratio", "warmup": 5, "source": "market.oi_slope_5m", "required": false},
    "oi_acceleration": {"type": "float", "unit": "ratio", "warmup": 10, "source": "derived.oi_accel", "required": false},
    "funding_rate": {"type": "float", "unit": "rate", "warmup": 0, "source": "prepared.funding_rate", "required": false},
    "funding_velocity": {"type": "float", "unit": "rate", "warmup": 5, "source": "market.funding_trend", "required": false},
    "ls_ratio": {"type": "float", "unit": "ratio", "warmup": 0, "source": "prepared.ls_ratio", "required": false},
    "global_ls_ratio": {"type": "float", "unit": "ratio", "warmup": 0, "source": "prepared.global_ls_ratio", "required": false},
    "depth_imbalance": {"type": "float", "unit": "ratio", "warmup": 0, "source": "prepared.depth_imbalance", "required": false},
    "microprice_bias": {"type": "float", "unit": "ratio", "warmup": 0, "source": "prepared.microprice_bias", "required": false},
    "basis_pct": {"type": "float", "unit": "percent", "warmup": 0, "source": "prepared.basis_pct", "required": false},
    "premium_zscore_5m": {"type": "float", "unit": "zscore", "warmup": 5, "source": "prepared.premium_zscore_5m", "required": false},
    "liquidation_score": {"type": "float", "unit": "score", "warmup": 0, "source": "prepared.liquidation_score", "required": false},
    "poc_migration_1h": {"type": "float", "unit": "pct", "warmup": 0, "source": "market.map_poc_migration_1h", "required": false},
    "poc_migration_4h": {"type": "float", "unit": "pct", "warmup": 0, "source": "market.map_poc_migration_4h", "required": false},
    "va_contraction": {"type": "float", "unit": "ratio", "warmup": 0, "source": "market.map_vp_va_contraction", "required": false},
    "liquidity_void_path": {"type": "float", "unit": "pct", "warmup": 0, "source": "market.map_void_above_pct", "required": false},
    "lifecycle_phase": {"type": "string", "unit": "enum", "warmup": 0, "source": "row.lifecycle.phase", "required": false},
    "lifecycle_bias": {"type": "string", "unit": "enum", "warmup": 0, "source": "row.lifecycle.recommended_bias", "required": false},
    "market_regime": {"type": "string", "unit": "enum", "warmup": 0, "source": "row.regime.market_regime", "required": false},
    "closed_bar": {"type": "bool", "unit": "flag", "warmup": 0, "source": "frame.closed_bar", "required": false}
  }
}
