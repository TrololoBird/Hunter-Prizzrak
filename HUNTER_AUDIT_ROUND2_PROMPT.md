# HUNTER — доаудит (round 2), резюмируемый по чанкам

**Проблема round 1:** прогон дважды упёрся в лимит сессии и не завершился — задача была неделимой. **Решение:** один чанк = один сеанс, append после КАЖДОГО файла, чекпойнт-файл. Обрыв теряет максимум один файл.

**Модель:** Fable 5. **Код не менять** — только отчёт.

---

## План чанков (запускать по одному за сеанс)

| # | Чанк | Файлы | ~Строк |
|---|---|---|---|
| 1 | `hunt_core/runtime/cycle/*` | ~5 | ~2 500 |
| 2 | `hunt_core/runtime/*` (остальное: tick_assembly, symbol_probe, analyst_assembly, services) | ~18 | ~4 800 |
| 3 | `hunt_core/track/*` (кроме уже покрытого tracker.py close/auto-resolve) | 14 | ~4 800 |
| 4 | `hunt_core/data/*` | 11 | ~3 900 |
| 5 | `hunt_core/levels/*` (levels.py = 1604) | 3 | ~1 700 |
| 6 | `hunt_core/toolkit/*` (кроме targets/forecast) + `hunt_core/params/*` | ~9 | ~1 800 |
| 7 | `hunt_core/domain/*` + `hunt_core/signals/*` + `hunt_core/diagnostics/*` | 15 | ~2 150 |
| 8 | root `hunt_core/*.py` (кроме `contract.py::worst_entry_edge` — покрыт) | 10 | ~2 500 |

---

## ПРОМТ (подставить номер чанка). Один чанк = один сеанс.

```
Ты — Fable 5 в Claude Code. Round 2 доаудита HUNTER. Работаешь РОВНО НАД ОДНИМ ЧАНКОМ и останавливаешься. НЕ МЕНЯЙ КОД.

ЧАНК: <номер и пути из плана, напр. «#1 — hunt_core/runtime/cycle/*»>

━━━ ПРОТОКОЛ РЕЗЮМИРУЕМОСТИ (соблюдать строго) ━━━
1. Прочитай docs/AUDIT_CHECKPOINT.md (если нет — создай по шаблону ниже). Возьми ПЕРВЫЙ чанк со статусом `pending` (или указанный мной). Поставь ему `in-progress`.
2. Иди ПОФАЙЛОВО. После КАЖДОГО файла: допиши карточку в docs/AUDIT_ROUND2.md (append, не переписывать) И обнови строку файла в чекпойнте. НЕ копи результаты в памяти до конца — обрыв не должен стоить больше одного файла.
3. Закончив чанк: статус `done`, число найденных gap'ов в чекпойнт, и СТОП. Следующий чанк не начинай.

Шаблон docs/AUDIT_CHECKPOINT.md:
| чанк | пути | статус | файлов покрыто | gap'ов |
|---|---|---|---|---|
| 1 | runtime/cycle/* | pending | 0/5 | — |
(…все 8 строк из плана…)

━━━ БАЗА (прочитать первым) ━━━
CLAUDE.md, AGENTS.md, docs/HUNTER_TARGET_SPEC.md (ОСОБЕННО §1 non-goals, §5 калибровочная поверхность, §8 gap-лист G-1..G-30 — это уже закрытое, НЕ переоткрывать), docs/MANIPULATION_METHODOLOGY*.md, research/prizrak_corpus/*.md.

КОНТЕКСТ: проект целиком сгенерирован ИИ по методикам Telegram-каналов «Призрак»/«Owner of Shorts»/«Спекулянтка». У кода НЕТ автора, знающего замысел. Владелец — не разработчик.
ПРИОРИТЕТ ИСТИНЫ (строго): 1) метод каналов; 2) внутренняя согласованность кода; 3) статьи/проекты — ТОЛЬКО санити-чек, НИКОГДА не источник фич.

━━━ ПРИЦЕЛЬНАЯ ОХОТА (round 1 доказал: эти классы в ЭТОМ коде продуктивны — искать их первыми) ━━━
Сквозной мотив round 1: **«неизвестное принималось за ноль/факт»**. Конкретно ищи:
• falsy-zero `or`-цепочки: `x or 0`, `x or default` где 0.0 — валидные данные (класс G-21);
• прокси-счётчики, врущие о жизни: `len(buf)` как «событий по символу» (G-15), кэш без age-гейта, отдающий протухшее как свежее (G-12);
• ФАНТОМ-КЛЮЧИ: поле читается, но НИКТО его не пишет (или пишет под другим именем) → ветка мертва навсегда. Round 1 так нашёл: BOS≡False (G-1), CHoCH никогда (G-10), `forward_zones` vs `liq_forward_zones` (G-8), `imb_1.0pct` vs `imb_1pct` (G-9), рендер-блоки в никуда (G-17), мёртвый `rr_conservative`-кап (G-23). Для КАЖДОГО читаемого ключа проверь grep'ом продюсера;
• окно/индекс включает текущий бар → предикат недостижим (G-1);
• LOOKAHEAD: as-of join без `strategy="backward"`, shift/rolling, тянущие будущее (G-29);
• имя врёт о семантике: `worst_*` возвращает best (G-3), «консенсус» усредняет разнотипные величины (G-20);
• двойной учёт: событие попадает и в общий, и в свой буфер (G-22);
• конфиг doc-only: TOML/env-секция задокументирована, но НИКЕМ не читается (прецедент 0644bd6/d3e3dcf);
• мёртвый код: ноль импортёров (grep по module-path, символам, lazy/importlib, tests/scripts/pyproject) (G-25);
• нарушения устава: stdlib `logging` вместо structlog, dataclass там, где value-модель (G-26);
• ОШИБКИ РАСЧЁТОВ: пере-вычисли КАЖДУЮ формулу с показом пересчёта (знак, off-by-one, нормировка, единицы, деление на ноль, инверсия направления — ср. G-11 fib `direction="down"` был no-op);
• контракт-рассинхрон через границы модулей; поля-сироты (производятся, никем не читаются).

━━━ КАРТОЧКА НА ФАЙЛ (append в docs/AUDIT_ROUND2.md) ━━━
### <путь к файлу>
- **Назначение** (1 строка) + какую часть метода канала реализует, или «нет методной опоры» (класс B).
- **Находки** (или «чисто»). На каждую:
  - `file:line` — верифицировать GREP'ом по содержимому (номера не выдумывать);
  - класс: A (противоречит методу, с цитатой) / B (нет методной опоры — намерение НЕИЗВЕСТНО, не додумывать) / C (внутреннее противоречие) / D (доставка/лейбл);
  - категория: bug / расчёт / логика / артефакт-заглушка-мёртвый-код / контракт / библиотеки;
  - **доказательство**: цитата метода, ИЛИ пересчёт, ИЛИ grep, показывающий отсутствие продюсера/импортёра;
  - **падающий тест или точный сценарий репро** — без него это ГИПОТЕЗА, помечай так;
  - severity: P0 (искажает живой сигнал/статистику) / P1 (контракт/честность лейблов) / P2 (техдолг/мёртвый код);
  - минимальный фикс НА ИМЕЮЩИХСЯ ДАННЫХ;
  - **signal-level before/after** — как изменится ТЕКСТ сигнала (владелец не разработчик).

Подтверждённые находки нумеруй как gap'ы, ПРОДОЛЖАЯ нумерацию спеки: **G-31, G-32, …** (G-1..G-30 заняты). Дублирующее уже закрытое — не заводить.

━━━ SELF-VERIFICATION (в конце чанка) ━━━
Перепроверь свои же `file:line` (заново grep) и пересчёты. Что не воспроизвёл — понизь до «гипотеза». Явно раздели «подтверждено» / «гипотеза».

━━━ ЗАПРЕТЫ ━━━
Код не менять. Бот не запускать/рестартить. data-plane/cron/lake не трогать. Новые слои/данные/зависимости — только с ПРОДЕМОНСТРИРОВАННЫМ провалом (necessity-бар §7: опционы/футпринт-слой/on-chain/спот/OFI/VPIN уже провалили — не предлагать). Неизвестное намерение (класс B) за факт не выдавать. Не переоткрывать G-1..G-30. Метод > код > статьи. Рефакторинг — только ПРЕДЛОЖЕНИЕМ с требованием behavior-preserving пиннинга, не правкой.

━━━ ВЫВОД ЧАНКА ━━━
В конце сеанса: короткая сводка (сколько файлов, сколько gap'ов по severity, топ-3 самых серьёзных), чекпойнт обновлён, статус чанка `done`. СТОП.
```

---

## Порядок
1. Прогони чанки **1→8**, по одному за сеанс. Обрыв не страшен: чекпойнт + append сохраняют прогресс, следующий сеанс подхватит с `pending`.
2. Собрав все 8 — объедини `docs/AUDIT_ROUND2.md` gap'ы (G-31+) в §8 спеки одним проходом.
3. Одобренное отдай **Промту 2 (Opus, исполнение)** из `HUNTER_AUDIT_PROMPTS.md` — двойной гейт, без переписывания.

> **Приоритет:** доаудит НЕ на критическом пути. Первым идёт **бэктест-гейт** (G-3/G-7 меняют эмиссию; первый прогон убит по памяти, exit 137) — до него абсолютным R не доверять. Доаудит — параллельно/после.
