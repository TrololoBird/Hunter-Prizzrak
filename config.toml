# Auto-discovered by hunt_core.market.network
[bot.network]
# Binance USDⓈ-M is reached on a DIRECT connection. The rotating egress proxy pool
# (proxy_urls / failover_*) was removed — a dead proxy used to hang every CCXT call.
trust_env = true